import PersonalInfo from './PersonalInfo';
import Projects from './Projects';

const CV = () => {
    return (
        <div>
            <PersonalInfo />
            <Projects />
        </div>
    );
};

export default CV;
